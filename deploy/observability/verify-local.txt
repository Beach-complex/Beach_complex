PR4 로컬 검증 명령 (저장소 루트에서 실행)
필요: Docker + Compose, Python 3, Terraform 1.15.8, cloud-init
AWS 접근/배포/commit/push 없이 실행한다.

# Terraform mock plan 3건, cloud-init, Compose, Loki/Alloy 설정 검사
bash deploy/observability/scripts/validate-iac.sh

# 실제 Docker 로그 수집 및 Grafana/Tempo 연결 검증, 테스트 리소스 자동 정리
python3 deploy/observability/scripts/verify-log-pipeline.py

# 결과
cat build/reports/observability/log-pipeline.json

# 변경 파일 공백 오류 확인
git diff --check

별도의 테스트 daemon을 권장한다. 기존 beach.logs=backend 컨테이너가 있으면 E2E는 중단된다.
검증 범위와 서버 적용 시 확인할 항목: deploy/observability/README.md
